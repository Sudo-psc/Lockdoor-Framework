[metadata]
# license_files = LICENSE # Uncomment if you have a LICENSE file at the root

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.8

[options.packages.find]
where = src
